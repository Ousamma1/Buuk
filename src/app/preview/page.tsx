import { SurveyPreviewClient } from "@/components/preview/SurveyPreviewClient";

export const metadata = {
  title: "Survey Preview | Buuk",
};

export default function PreviewPage() {
  return <SurveyPreviewClient />;
}
