import { SurveyBuilderClient } from "@/components/builder/SurveyBuilderClient";

export const metadata = {
  title: "Survey Builder | Buuk",
};

export default function BuilderPage() {
  return <SurveyBuilderClient />;
}
